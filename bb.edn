{:paths ["src"]
 :tasks
 {:override-builtin true
  :init (do (require '[babashka.process :refer [shell]]))

  :test {:doc "Run unit tests (tools.deps alias :test)"
         :task (shell "clj -M:test")}

  :run  {:doc "Run -main in decision-agent.core via tools.deps"
         :task (shell "clj -M:run")}}}
